# seasonalnet-web

SeasonalNet frontend monorepo.

## Apps
- `apps/www`
- `apps/radio`
- `apps/pbx`
- `apps/prov`
- `apps/admin`

## Notes
- This repository was initialized from the live SeasonalWeb SPA deployments.
- Shared shell and UI code will be extracted into `packages/` after the initial import.
- Documentation content lives in `seasonalnet-docs`; docs-app glue will live here later.
